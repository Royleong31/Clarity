/* eslint-disable @typescript-eslint/no-explicit-any */
declare module '@ensdomains/thorin' {
    // Add specific types or use 'any' as a temporary solution
    const Helper: any;
    const Typography: any;
    const lightTheme: any;
    const darkTheme: any;
    const Spinner: any;
    const address: any;
    const chain: Chain;
    const resolver: any;
    const Button: any;
    const mq: any;
    export const { Helper, Typography, mq, lightTheme, darkTheme, Spinner, address, chain, resolver, Button };
}